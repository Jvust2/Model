name: runtime-package-hotfix

on:
  push:
    branches:
      - fix/windows-launcher-localsite

permissions:
  contents: read

jobs:
  package:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Parse launcher
        shell: powershell
        run: |
          $tokens = $null
          $errors = $null
          [System.Management.Automation.Language.Parser]::ParseFile(
            (Resolve-Path "runtime\launch_model.ps1"),
            [ref]$tokens,
            [ref]$errors
          ) | Out-Null
          if ($errors.Count -gt 0) {
            $errors | ForEach-Object { Write-Error $_.Message }
            exit 1
          }

      - name: Build runtime ZIP
        shell: powershell
        run: |
          $root = "Model-Runtime"
          New-Item -ItemType Directory -Force -Path $root | Out-Null
          Copy-Item -Recurse -Force runtime "$root\runtime"
          Copy-Item -Force README.md "$root\README.md"
          Copy-Item -Force index.html "$root\index.html"
          Copy-Item -Recurse -Force assets "$root\assets"
          Copy-Item -Force sw.js "$root\sw.js"
          Compress-Archive -Path "$root\*" -DestinationPath "Model-Runtime-Windows-fixed.zip" -Force

      - name: Upload runtime ZIP
        uses: actions/upload-artifact@v4
        with:
          name: Model-Runtime-Windows-fixed
          path: Model-Runtime-Windows-fixed.zip
          if-no-files-found: error
